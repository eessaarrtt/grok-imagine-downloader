#!/bin/bash

# Скрипт для загрузки расширения на GitHub

echo "🚀 Подготовка к загрузке на GitHub..."
echo ""

# Проверка, что мы в правильной директории
if [ ! -f "manifest.json" ]; then
    echo "❌ Ошибка: manifest.json не найден. Запусти скрипт из папки расширения."
    exit 1
fi

# Инициализация git (если еще не инициализирован)
if [ ! -d ".git" ]; then
    echo "📦 Инициализация git репозитория..."
    git init
    echo "✅ Git репозиторий инициализирован"
fi

# Добавление всех файлов
echo "📝 Добавление файлов..."
git add .

# Коммит
echo "💾 Создание коммита..."
git commit -m "Initial commit: Grok Imagine Image Downloader extension" || echo "⚠️  Коммит уже существует или нет изменений"

# Переименование ветки в main (если нужно)
git branch -M main 2>/dev/null || true

echo ""
echo "✅ Локальный репозиторий готов!"
echo ""
echo "📋 СЛЕДУЮЩИЕ ШАГИ:"
echo ""
echo "1. Создай новый репозиторий на GitHub:"
echo "   - Зайди на github.com"
echo "   - Нажми 'New repository'"
echo "   - Название: grok-imagine-downloader (или любое другое)"
echo "   - Сделай публичным (Public)"
echo "   - НЕ создавай README, .gitignore и лицензию"
echo ""
echo "2. После создания репозитория выполни:"
echo "   git remote add origin https://github.com/ТВОЙ_USERNAME/grok-imagine-downloader.git"
echo "   git push -u origin main"
echo ""
echo "   (Замени ТВОЙ_USERNAME на свой GitHub username)"
echo ""

